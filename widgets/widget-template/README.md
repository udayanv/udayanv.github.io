# Widget name

One sentence describing the task this widget helps a person complete.

## Status

Starter — replace this text before adding the widget to the catalog.

## Run locally

Serve the parent `widgets/` directory with a static HTTP server, then open this directory’s URL. The widget has no install or build step.

## Structure

- `index.html` — document structure and metadata
- `styles.css` — widget-specific presentation
- `script.js` — widget behavior

This starter imports the shared tokens and baseline styles. Keep styles and behavior that are unique to this widget in this directory.

