const action = document.querySelector("#example-action");
const status = document.querySelector("#example-status");

action.addEventListener("click", () => {
  status.textContent = "The starter interaction works. Replace it with your widget.";
});

